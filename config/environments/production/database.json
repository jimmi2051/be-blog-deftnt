{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "mongoose",
      "settings": {
        "host": "${process.env.DATABASE_HOST || 'ds235840.mlab.com'}",
        "port": "${process.env.DATABASE_PORT || 35840}",
        "database": "${process.env.DATABASE_NAME || 'be-blog-deftnt'}",
        "username": "${process.env.DATABASE_USERNAME || 'root'}",
        "password": "${process.env.DATABASE_PASSWORD || 'o0i9u8y7'}",
        "srv": false
      },
      "options": {
        "authenticationDatabase": "be-blog-deftnt",
        "ssl": false
      }
    }
  }
}
